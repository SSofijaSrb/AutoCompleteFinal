// Source code is decompiled from a .class file using FernFlower decompiler.
package autocomplete;

import java.io.File;
import java.util.List;
import java.util.Scanner;

public class AutoCompleteApp {
   public AutoCompleteApp() {
   }

   public static void main(String[] var0) {
      Trie var1 = new Trie();
      loadWordsFromFile(var1, "words.txt");
      System.out.println("Autocomplete loaded!");
      Scanner var2 = new Scanner(System.in);

      while(true) {
         System.out.print("Enter a prefix (or type 'exit'): ");
         String var3 = var2.nextLine();
         if (var3.equals("exit")) {
            var2.close();
            return;
         }

         List var4 = var1.autoComplete(var3);
         System.out.println("Suggestions: " + var4);
      }
   }

   private static void loadWordsFromFile(Trie var0, String var1) {
      try {
         Scanner var2 = new Scanner(new File(var1));

         try {
            while(var2.hasNextLine()) {
               String var3 = var2.nextLine().trim();
               if (!var3.isEmpty()) {
                  var0.insert(var3.toLowerCase());
               }
            }
         } catch (Throwable var6) {
            try {
               var2.close();
            } catch (Throwable var5) {
               var6.addSuppressed(var5);
            }

            throw var6;
         }

         var2.close();
      } catch (Exception var7) {
         System.err.println("Error loading dictionary: " + var7.getMessage());
      }

   }
}
